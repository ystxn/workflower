id: abc
activities:
  - send-message:
      id: init
      on:
        message-received:
          content: hello
      content: |
        hihihix

  - update-message:
      id: aloha
      on:
        form-replied:
          form-id: init
      message-id: ${init.outputs.msgId}
      content: Hey